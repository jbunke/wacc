stages:
- build
- frontend_tests
- backend_tests

build:
  stage: build
  script: make
  artifacts:
    paths:
      - src/
      - bin/

frontend_tests:
  stage: frontend_tests
  script: bash tests/frontend_test

backend_tests:
  stage: backend_tests
  script: bash tests/backend_test
